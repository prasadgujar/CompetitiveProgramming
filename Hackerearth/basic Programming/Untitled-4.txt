#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    cin>>s;
    int n = s.length();
    int sum = 0;
    for(int i=0;i<n;i++)
    {
        if(isdigit(s[i]))
        {
            sum +=stoi(s[i]);
        }
        else if
    }    
}
int main() {
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
	
	return 0;
}